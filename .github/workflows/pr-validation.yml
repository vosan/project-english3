name: PR Validation + Preview Deployment

on:
  pull_request:
    branches: ["main"]

permissions:
  contents: read
  pages: write
  id-token: write
  pull-requests: write

jobs:
  validate-and-preview:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Cache NPM
        uses: actions/cache@v3
        with:
          path: ~/.npm
          key: ${{ runner.os }}-node-${{ hashFiles('package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-

      - name: Install dependencies
        run: npm install

      - name: Check format
        run: npm run check-format

      - name: Build site with Gulp
        run: npx gulp build

      - name: Copy build to PR folder
        run: |
          PR_NUM=${{ github.event.pull_request.number }}
          mkdir -p ./dist/pr-$PR_NUM
          cp -r ./dist/* ./dist/pr-$PR_NUM/

      - name: Cleanup old PR previews
        run: |
          cd dist
          folders_to_delete=$(ls -1d pr-* | sort -V | head -n -10)
          if [ ! -z "$folders_to_delete" ]; then
            rm -rf $folders_to_delete
          fi

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./dist

      - name: Deploy PR preview
        id: deployment
        uses: actions/deploy-pages@v4

      - name: Comment PR preview URL
        if: github.event.pull_request.head.repo.full_name == github.repository
        uses: actions/github-script@v6
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            github.rest.issues.createComment({
              issue_number: context.payload.pull_request.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: `Preview URL: ${{ steps.deployment.outputs.page_url }}/pr-${{ github.event.pull_request.number }}/`
            })
